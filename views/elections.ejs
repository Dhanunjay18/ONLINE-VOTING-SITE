<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="./css/styles.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <title> Voting Platform </title>
</head>
<body class="bg-blue-300">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
                <%- include('header.ejs') %>
            <br>
            <h1 class="font-semibold text-2xl"> Admin ID : <%= name %>! </h1>
            <hr> <br>
            <% if(elections.length === 0 ) { %>
                <h3>You Haven't Created any Elections </h3>
                <% } else { %>
                    <div>
                        <%- include('list.ejs', {title:'Created Elections', data: elections}) %>
                    </div>
                    <% } %>
                    <a href="/new" style="color:blue"> Click here to create an election </a>
                    <% if(messages && messages.error && messages.error.length > 0)  { %>
                        <% for(var i=0; i<messages.error.length; i++) { %>
                            <li class="text-red-600"> <%= messages.error[i] %></li>
                            <% } %>
                            <% } %>
                            <br><br>
                            <p style="float:left;" class="border rounded bg-rose-600 border-solid text-white rounded"><a href="/signout">Sign Out</a></p>
                    </div>
</body>
</html>