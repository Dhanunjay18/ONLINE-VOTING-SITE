<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="./css/styles.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <title> Voting Platform </title>
</head>
<body class="bg-blue-300">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
            <p style="float: right;" class="border rounded bg-rose-600 border-solid text-white rounded"><a href="/signout">Sign Out</a></p>
            <h1 class="font-semibold text-2xl"> Welcome <%= questions.title %>! </h1>
            <% if(questions.length === 0 ) { %>
                <h3>You Haven't Created any Questions Yet </h3>
                <% } else { %>
                    <div>
                        <%- include('listQuestion.ejs', {title:'Created questions', data: questions}) %>
                    </div>
                <% } %>
                <a href="/elections/<%= election.id %>/questions" style="color:blue"> Click here to create the Question </a>
                <br>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                        <li class="text-red-600"> <%= messages.error[i] %></li>
                <% } %>
              <% } %>
            
    </div>
</body>
</html>