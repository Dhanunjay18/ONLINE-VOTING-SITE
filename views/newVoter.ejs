<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="./css/styles.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
<body class="bg-blue-300">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <h1 class="px-2" style="font-weight: bolder;font-size: larger;"> Enter Voted ID and Password </h1>
            <form action="/elections/<%= eid %>/voters" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <div class="flex py-2">
                    <div class="flex-auto px-2">
                        <input name="vid" type="text" placeholder="Enter Voter ID" class="border border-solid text-black px-2 w-full rounded" />
                    </div>
                    <div class="flex-auto">
                        <input name="vpass" type="password" class="border leading-6 border-solid text-black px-2 w-full rounded"/>
                    </div>
                    <div class="flex-none px-2">
                        <button type="submit" class="border rounded bg-rose-600 border-solid text-white px-2 w-full rounded">Add</button>                        
                    </div>
                </div>
            </form>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                        <li class="text-red-600"> <%= messages.error[i] %></li>
                <% } %>
              <% } %>
              <div>
                <%- include('voters.ejs', {title:'Registered Voters', data: voters}) %>
              </div>
              <p style="float: left;" class="border rounded bg-rose-600 border-solid text-white rounded"><a href="/signout">Sign Out</a></p>
    </div>
</body>
</html>